import React, { ReactNode } from 'react';

export default function ToastProvider({ children }: { children: ReactNode }) {
  // Minimal placeholder to satisfy imports. Replace with real implementation if needed.
  return <>{children}</>;
}
